using UnityEngine;

public class MissionManager
{
    public enum MissionType { DODGE, FEED, BLIEND, NONE};
    MissionType missionType = MissionType.NONE;

    void Dodge(ObstacleManager obstacleManager)
    {
        
    }
}
